# Program to create the preprocessed files from the original files of the Shared Task

First, adjust the paths contained in the file. 

Then, execute the following command:

```
python3 preproc.py
```
